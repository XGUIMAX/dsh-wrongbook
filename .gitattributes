# Keep line endings stable across platforms: the plugin is plain JS, and a
# CRLF/LF flip would otherwise rewrite every line of every file.
* text=auto eol=lf

*.png binary
*.jpg binary
*.webp binary
*.gif binary
